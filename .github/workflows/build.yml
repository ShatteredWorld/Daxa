name: Build

on:
  push:
    branches: [ "personal" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Debug, RelWithDebInfo, Release]
        c_compiler: [gcc, clang, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            os_name: windows
          - os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++
            os_name: linux
          - os: ubuntu-latest
            c_compiler: clang
            cpp_compiler: clang++
            os_name: linux
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-latest
            c_compiler: cl

    steps:
    - uses: actions/checkout@v4

    - name: Cache CMake build
      uses: actions/cache@v4
      with:
        path: |
          ${{ github.workspace }}/build
          ~/.ccache
        key: ${{ runner.os }}-${{ hashFiles('**/*.cmake') }}

    - name: Setup Vulkan SDK
      uses: humbletim/setup-vulkan-sdk@v1.2.1
      with:
        vulkan-query-version: 1.3.296.0
        vulkan-components: Vulkan-Headers, Vulkan-Loader, Glslang, SPIRV-Headers, SPIRV-Tools
        vulkan-use-cache: true

    - name: Install Linux dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
        sudo apt-get update

        sudo apt-get install -y gcc-14 g++-14 clang-19 libwayland-dev libxkbcommon-dev xorg-dev libxcb1-dev
        
        sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
        sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
        sudo update-alternatives --install /usr/bin/clang clang /usr/bin/clang-19 100
        sudo update-alternatives --install /usr/bin/clang++ clang++ /usr/bin/clang++-19 100

        echo "/usr/lib/ccache" >> $GITHUB_PATH

    - name: Set reusable strings
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -DDAXA_ENABLE_UTILS_FSR3=false
        -DDAXA_ENABLE_UTILS_IMGUI=false
        -DDAXA_ENABLE_UTILS_MEM=false
        -DDAXA_ENABLE_UTILS_PIPELINE_MANAGER_GLSLANG=false
        -DDAXA_ENABLE_UTILS_PIPELINE_MANAGER_SLANG=false
        -DDAXA_ENABLE_UTILS_PIPELINE_MANAGER_SPIRV_VALIDATION=false
        -DDAXA_ENABLE_UTILS_TASK_GRAPH=false
        -DDAXA_ENABLE_TESTS=false
        -DDAXA_ENABLE_TOOLS=false
        -DDAXA_ENABLE_STATIC_ANALYSIS=false
        -DDAXA_VALIDATION=1
        -S ${{ github.workspace }}

    - name: Build CMake
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }} --target daxa

    - name: Install
      run: cmake --install ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }} --prefix ${{ github.workspace }}/install

    - name: Package build artifacts
      shell: bash
      run: |
        if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
          cd "${{ github.workspace }}/install"
          7z a -tzip "${{ github.workspace }}/${{ matrix.os_name }}-${{ matrix.c_compiler }}-${{ matrix.build_type }}-pm${{ matrix.pipeline_manager }}.zip" .
        else
          cd "${{ github.workspace }}/install"
          zip -r "${{ github.workspace }}/${{ matrix.os_name }}-${{ matrix.c_compiler }}-${{ matrix.build_type }}-pm${{ matrix.pipeline_manager }}.zip" .
        fi

    - name: Upload build artifacts
      if: matrix.config == 'Release'
      uses: actions/upload-artifact@v4
      with:
        retention-days: 14
        name: ${{ matrix.os_name }}-${{ matrix.c_compiler }}-${{ matrix.build_type }}
        path: ${{ github.workspace }}/${{ matrix.os_name }}-${{ matrix.c_compiler }}-${{ matrix.build_type }}.zip
